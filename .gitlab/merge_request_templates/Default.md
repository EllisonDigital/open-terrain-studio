## What this changes

## How it was tested

- [ ] `cargo fmt`, `cargo clippy -- -D warnings` and `cargo test` pass
- [ ] Smoke test passes (`godot --headless --path app --script res://tests/smoke_test.gd`)
- [ ] Every commit is signed off (`git commit -s`)
- [ ] New nodes use metres, not pixels, and have a test
