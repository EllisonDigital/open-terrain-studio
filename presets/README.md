# Presets

Example projects (the v0.2 reference landforms: alpine range, canyon, dune
field) live in `app/examples/` so the packaged app can open them from
*File → Open Example*.

Graph templates and species presets (YAML) arrive in v0.7–v0.8. See `docs/ROADMAP.md`.
