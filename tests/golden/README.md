# Golden references

Reference outputs (EXR) for regression tests are stored here via Git LFS from
v0.2 onwards. v0.1 pins exact noise values in
`crates/terrain-nodes/src/noise/basis.rs` (`golden_values_never_change`).
